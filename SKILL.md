---
name: image-prompt-analyzer
description: 将参考图片、海报、绘本页面或已有图片提示词拆解为主体、位置、构图、背景、材质、灯光、镜头、文案留白、固定项和可替换项，并输出可直接粘贴到 ChatGPT Image 2.5 的自然语言生成或编辑提示词。用于参考图反向分析、系列视觉模板、图片生成提示词重写、局部编辑说明和批量页面提示词整理。
---

# 图片提示词分析器

## 任务目标

把视觉输入转换为**可执行、可复用、可批量变体**的图片提示词。核心不是复述画面，而是提炼空间关系、视觉优先级、可控变量和必须保持的约束。

## 总体流程

按以下顺序执行：

1. **判断任务**：识别是参考图拆解、已有提示词重写、图片编辑、系列模板，还是批量页面整理；确认画幅、用途和是否需要文案留白。
2. **分离观察与推断**：把结论标成“直接观察”“合理推断”或“待确认”；没有参考图时，将灯光、镜头和材质写成建议约束，不伪装成观察事实。
3. **拆解视觉结构**：依次分析主体、位置、构图、背景、材质、灯光、镜头、色彩、文案留白。
4. **建立变体模型**：分开固定项和可替换项，并为每个可替换项写出范围及不可破坏的约束。
5. **生成最终提示词**：将结果压缩为一段可直接提供给 ChatGPT Image 2.5 的自然语言提示词。
6. **执行质检**：检查动作合理性、空间关系、文案安全区、人物/产品一致性、文字风险和版权边界。

## 视觉拆解标准

### 主体与动作

回答“主体是谁/是什么、正在做什么”：主体类别、数量、主次、身份或年龄感、姿态、动作、视线、关键轮廓、服饰、颜色和道具。说明主体占画面的粗略比例及视觉焦点来源。不要在不可见处虚构身份、年龄、品牌型号、功能或故事背景。

### 位置与空间层级

使用左/中/右、上/中/下、前景/中景/背景描述主体及关键元素。补充主体与边缘、文案区、其他主体的距离，是否出血，是否遮挡，以及朝向和运动方向。

### 构图与视觉动线

说明画面骨架（居中、偏置、对称、对角线、框景、拼贴或留白）、元素大小层级、观众的观看顺序、裁切方式、负空间形状与用途、景深和清晰度。不要只写“黄金分割”或“高级构图”，要说明具体空间关系。

### 背景与环境

说明场景类型、背景色阶、空间深度、纹理、必要道具、环境叙事及虚实关系。区分必须保留的环境线索和可删减的装饰；确认背景是否帮助主体轮廓分离和文案可读。

### 材质与表面

分别描述主体、背景和关键道具的材质。使用可见线索说明哑光/镜面、反射、折射、透光、粗糙度、颗粒、褶皱、湿润高光、边缘磨损和纸张纹理；不要只写“质感强”。

### 灯光与阴影

说明主光从左/右、上/下、前/后哪个方向来，光质、色温、阴影方向和软硬、高光位置、接触阴影、轮廓光及主体与背景的明暗关系。方向不确定时写“疑似……，需由阴影验证”或改写为生成约束。

### 镜头与观看距离

说明景别（远景、全景、中景、半身、近景、特写、微距）、视角（平视、俯拍、仰拍、顶视、三分之四侧面）、透视强弱、背景压缩、边缘变形、焦点和景深。仅在证据充分时写具体焦段，否则使用广角/标准/长焦倾向。

### 文案留白

说明文案区域、占比、连续性、安全距离、预期层级和背景对比度。未提供真实文案时写“不要生成文字，预留后期排版区域”；不要擅自编造标题、品牌名、版权信息或正文。

## 固定项与可替换项

### 固定项

通常包括：画幅比例、主体核心轮廓与身份、主体相对位置、构图骨架、视觉焦点、文案安全区、品牌色或主色、光线方向、景别、关键标识、媒介质感和整体叙事气质。只把用户明确要求或系列一致性真正需要的内容列为固定项，避免过度限制。

### 可替换项

通常包括：人物外观与服装颜色、产品型号或颜色、动作、道具、背景场景、季节、辅助色、纹理、材质细节和短期文案。每项必须写允许范围及约束，例如：“可替换背景为森林、花园或室内，但保持主体轮廓分离、左上方光线和标题安全区不变。”

## ChatGPT Image 2.5 输出规范

默认最终交付是一段**可直接粘贴到 ChatGPT Image 2.5 输入框的自然语言提示词**。使用以下包裹格式：

```text
IMAGE_PROMPT
[一段完整的自然语言提示词]
END_IMAGE_PROMPT
```

遵守以下规则：

1. 使用自然语言，不使用 `--ar`、`--v`、权重括号、JSON、参数串或其他模型专用语法。
2. 按“画幅/用途 → 主体与动作 → 位置与比例 → 构图与动线 → 背景 → 材质 → 灯光 → 镜头 → 色彩与风格 → 文案留白 → 限制条件”组织内容。
3. 将一张图的要求整合为一段；不要把最终提示词写成表格或分散成关键词列表。
4. 图片编辑任务以“编辑这张图片”或“以提供的参考图为基础”开头，明确“保留什么、修改什么、不得改变什么”。
5. 需要准确文字时，将用户提供的文字原样放在引号中，并说明位置、层级和排版；未提供文字时，明确后期排版。
6. 人物、产品或角色一致性使用“保持……不变”的自然语言表达，不依赖不确定的技术参数。
7. 限制条件放在结尾，用“避免……”表达；重点限制动作安全、年龄合理、肢体完整、背景不拥挤、无水印和无未经要求的文字。
8. 用户说“只要提示词”时，仅输出 `IMAGE_PROMPT` 区块；用户要逐页分析或批量文档时，先给简短拆解，再按页输出独立区块。

## 参考图、版权与安全边界

当用户上传图片时，只分析和转写用户提供的内容；不要声称能够读取未提供的页面。拼图或低清图需要说明识别出的画面单元数量和不确定区域。对于现有绘本、电影、艺术家或品牌的请求，提炼用户提供画面的通用视觉结构，并将角色、服饰、道具、构图或场景做足够原创化调整；不要输出整本作品的逐页复原，也不要生成原作标题、正文、版权信息或 Logo。涉及婴幼儿时，动作必须符合年龄，避免高处、深水、火焰、尖锐物品和其他危险行为。

## 批量系列任务

批量生成前先建立“系列锁定表”：

- 固定人物/产品外观与核心轮廓；
- 固定服饰、主色、画幅和媒介质感；
- 固定主体相对位置、景别和文案安全区；
- 固定主光方向、阴影逻辑和背景复杂度；
- 为每页只替换动作、道具、场景或季节。

每个页面单独输出一个 `IMAGE_PROMPT`，不要把多个页面的动作和主体混在同一条提示词中。

## 推荐交付结构

除非用户要求“只要提示词”，按以下顺序输出：

1. **一句话视觉摘要**；
2. **画面结构表**：画幅、主体、位置、构图、背景、材质、灯光、镜头、文案留白和置信度/依据；
3. **固定项**；
4. **可替换项及约束**；
5. **ChatGPT Image 2.5 最终提示词**；
6. **风险与待确认项**。

结构表模板：

| 维度 | 分析结果 | 置信度/依据 |
|---|---|---|
| 画幅与用途 |  |  |
| 主体与动作 |  |  |
| 主体位置与比例 |  |  |
| 构图与视觉动线 |  |  |
| 背景与环境 |  |  |
| 材质与表面 |  |  |
| 光线与阴影 |  |  |
| 镜头、景别与视角 |  |  |
| 文案留白 |  |  |
| 色彩与媒介风格 |  |  |

## 交付前质检

逐项确认：

- 主体、动作、位置、比例和前后层级是否明确；
- 构图是否写出了空间关系和视觉动线；
- 背景、材质、光线方向、阴影和镜头是否可执行；
- 文案区域是否有位置、面积、对比度和安全距离；
- 固定项与可替换项是否分开，替换是否有边界；
- 事实、推断和待确认项是否区分；
- 提示词是否为一段自然语言并包含 `IMAGE_PROMPT` 标记；
- 是否避免其他模型参数、未经要求的文字、Logo、水印和版权信息；
- 人物动作、年龄表现和安全性是否合理；
- 批量页面是否保持系列锁定项一致。
